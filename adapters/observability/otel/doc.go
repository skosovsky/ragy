// Package otel provides OpenTelemetry decorators for ragy interfaces (Retriever, VectorStore, SemanticCache).
// It lives in a separate module so the ragy core stays free of third-party runtime dependencies.
package otel
